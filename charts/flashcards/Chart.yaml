apiVersion: v2
name: flashcards-app
description: Flashcards REST API application
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  # MongoDB database
  - name: mongodb
    version: 1.0.0
    repository: file://./charts/mongodb
    condition: mongodb.enabled