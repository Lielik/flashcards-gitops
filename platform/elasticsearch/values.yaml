elasticsearch:
  replicas: 3

  esJavaOpts: "-Xmx512m -Xms512m"

  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: 1000m
      memory: 2Gi

  # Disable security for simplicity (no user/pass)
  esConfig:
    elasticsearch.yml: |
      xpack.security.enabled: false