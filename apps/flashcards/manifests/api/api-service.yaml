apiVersion: v1
kind: Service
metadata:
  name: flashcards-api
  namespace: apps
  labels:
    app: flashcards
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: flashcards
    component: api
