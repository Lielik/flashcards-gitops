get the argocd password
kubectl -n argocd get secret argocd-initial-admin-secret \
  -o jsonpath="{.data.password}" | base64 -d

how to portforwad to argocd
kubectl port-forward svc/argocd-server -n argocd 8080:80